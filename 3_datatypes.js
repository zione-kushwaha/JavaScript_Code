"use strict"// treat all the JS code as newer version

// alert(3+3)  ------> we are using the node js ,not browser

console.log(3+
    3)    // this is also works fine but code readability should be high 

    console.log('jeevan');   //here the use of the ";" is optional we can but 
                            // it is generally not used 

    let names='jeevan'
    let age=20
    let RelationshipStatus =false
    
    console.table([typeof names,typeof age,typeof RelationshipStatus])

    //new things to be remember null type is the type of the object 
    // but the undefine type is undefine 

    console.log(typeof null)
    console.log(typeof undefined)
